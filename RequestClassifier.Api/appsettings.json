{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=RequestClassifierDb;Trusted_Connection=True;TrustServerCertificate=True"
  },

  "Jwt": {
    "Key": "RequestClassifier-Development-Secret-Key-2026-Change-Me", // The secret key used to sign and validate the JWT token
    "Issuer": "RequestClassifier.Api", // The application or authority that issues(creates) the token.
    "Audience": "RequestClassifier.Client", // The intended recipient or application that will use the token.
    "ExpirationMinutes": 60
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
